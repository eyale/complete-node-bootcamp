extends baseEmail

block content
  p Hi #{firstName},
  p Forgot password? Submit reset password with new password. Confirm to: #{url}.
  p (Action not implemented)
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=`${url}`, target='_blank') Reset password
  p - Ignore email if it was not you 🤷🏻‍♂️
